
#--------------------配置 service discovery------------
spring:
  application:
    name: eureka-client-consumer-feign
server:
  port: 8013

eureka:
  instance:
    hostname: localhost
  client:
     serviceUrl:
      defaultZone: http://localhost:8000/eureka/