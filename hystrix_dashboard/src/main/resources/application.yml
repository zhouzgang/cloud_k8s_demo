
#--------------------配置 service discovery------------
spring:
  application:
    name: hystrix-dashboard
server:
  port: 8017

feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/